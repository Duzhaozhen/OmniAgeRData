% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getOmniAgeData.R
\name{getOmniAgeData}
\alias{getOmniAgeData}
\title{Get OmniAgeR Data Resources}
\usage{
getOmniAgeData(title, localTest = FALSE, localDir = "local_test_data")
}
\arguments{
\item{title}{The title or ResourceName of the dataset.}

\item{localTest}{Logical. For internal developer use only.
Set to TRUE for local package testing before Bioconductor publication.
Default is FALSE.}

\item{localDir}{Character. For internal developer use only.
The path to the local data folder if localTest is TRUE.}
}
\value{
The loaded R object.
}
\description{
Get OmniAgeR Data Resources
}
\examples{
# 1. Mock a local environment for BiocCheck to run
# In a real scenario, you would have your data in a local folder
local_dir <- tempdir()
saveRDS(matrix(1:10), file.path(local_dir, "test_data.rds"))

# 2. Run a local test (this is "runnable" and satisfies BiocCheck)
try({
    res <- getOmniAgeData("test_data",
                          localTest = TRUE,
                          localDir = local_dir)
    print(res)
})

# 3. The official cloud usage
if (interactive()) {
    lung_data <- getOmniAgeData("omniager_lung_inv")
}

}
